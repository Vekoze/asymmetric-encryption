#ifndef HASH_HEADER
#define HASH_HEADER

#include <iostream>
#include <string>
#include <fstream>
#include <vector>

#define M 16
#define N 100

using namespace std;

string hash_file(string path);

#endif